/**
 * Il sito di footballer.it, da cui TrovaCampo si raggiunge e a cui si torna.
 *
 * Sta in un posto solo perché i collegamenti sono due (il menu utente e la
 * pagina Cerca) e devono portare nello stesso posto: se un giorno l'indirizzo
 * cambia, si cambia qui.
 */
export const SITO_FOOTBALLER = 'https://footballer.it';
